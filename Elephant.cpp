#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n, sum;
    cin >> n;
    if(n / 5 == 0)
        n = sum;
    cout << (n);
    return 0;
}
